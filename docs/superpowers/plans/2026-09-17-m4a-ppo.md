# M4a PPO 구현 계획 — 학생이 스스로 법규를 배우게 한다

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** M3 의 모방학습 학생을 출발점으로 PPO 를 돌려, 완주는 유지하면서 **신호 위반과 보호구역 과속을 줄인다**. 보상은 M2b 환경이 심판 감점으로 매기는 그 값이다.

**Architecture:** `vtd_rl/rl/` 이 다섯 조각이다. `actor_critic.py` 는 M3 정책망에 가치 머리를 붙이고 M3 체크포인트를 이식한다. `vec_env.py` 는 환경 여러 개를 프로세스로 띄운다. `buffer.py` 는 굴린 자취와 GAE 이점을 담고, `ppo.py` 는 잘라낸 정책 손실·가치 손실·엔트로피에 **모방 손실(감쇠)** 을 더한다. `scripts/train_ppo.py` 가 가치망만 먼저 데우고(스펙 §6.3) 본 학습을 돌린다. 평가는 M3 의 `vtd_rl/policy/evaluate.py` 를 그대로 쓰되 점수 집계만 고친다.

**Tech Stack:** Python 3.10, PyTorch 2.14, Gymnasium(벡터 환경), numpy 1.26.4, pytest. 학습 머신은 lab-main(RTX 3060, 16 코어). 두 머신 운용은 M4b.

**Spec:** `docs/superpowers/specs/2026-09-15-vtd-rl-urban-driving-design.md` (§4 관측·행동, §5 보상, §6.3 PPO, §6.4 진급 기준, §11 M4)

## Global Constraints

- 커밋 메시지에 Claude 표기(`Co-Authored-By`, `Claude-Session`, "Generated with")를 **넣지 않는다**.
- 규칙 스택 서브모듈(`third_party/rule_stack`)은 **수정하지 않는다**.
- 공개 레포에 주행 CSV, `runs/`, 체크포인트(`*.pt`), 데이터 조각(`*.npz`)을 커밋하지 않는다. 성적표(md)만 커밋한다.
- **numpy 1.26.4 고정, torch >=2.4,<3.**
- 테스트는 `env -u PYTHONPATH .venv/bin/pytest` 로 돌리고 **종료 코드 0** 으로 판정한다. 학습이 도는 테스트는 몇 초 안에 끝나는 크기로 만들고 `@pytest.mark.slow` 를 붙인다.
- **심판·채점기·세계·환경(`vtd_rl/env/`, `vtd_rl/referee/`, `vtd_rl/world/`)의 문턱과 규칙은 바꾸지 않는다.** 보상 계수(`RewardConfig`)도 이 계획에서는 건드리지 않는다 — PPO 가 지금 보상으로 무엇을 배우는지 먼저 본다.
- 학습·평가 산출물은 `runs/<머신>/<날짜>-<이름>/` 아래. 같은 폴더에 두 번 쓰지 않는다(M3 의 `--out` 가드와 같은 규칙).
- **모든 무작위는 시드로 고정한다** — 망 초기화, 환경, 배치 순서. 성적표에 시드를 적는다(M3 의 미고정 실행이 0%/100% 로 갈린 전례가 있다).
- **기준을 낮추지 않는다.** 목표에 못 미치면 숫자와 관찰을 성적표에 적는다.

## M3 이 넘긴 숙제(이 계획에서 처리한다)

1. **점수 기준이 사실상 통과 못 할 수 없다.** 멈춰 선 차는 위반을 안 해 97~100점이 나온다(M3 라운드 0: 완주율 0%, 점수 97.6). 완주 못 한 판은 **0점**으로 집계하도록 고친다(작업 1). 스펙 §6.4 의 진급 기준도 같은 뜻으로 고친다.
2. **σ 하한이 clamp 라 바닥에서 기울기가 0이다.** M3 체크포인트의 `log_std[0]` 은 이미 −2.0024로 바닥 밑에 있어 탐색 폭이 영원히 0.135에 묶인다. 파라미터를 직접 쓰되 **최적화 한 걸음 뒤 in-place 로 자른다**(기울기는 살아 있다). 상한도 둔다(작업 1).
3. **표본이 클리핑만 하고 밀도 보정이 없다.** PPO 는 **자른 적 없는 원래 표본과 그 로그확률**을 저장하고, 환경에 넣을 때만 자른다(작업 1).
4. **`DrivePolicy.load` 가 strict 다.** 가치 머리가 붙은 체크포인트를 읽으려면 `strict=False` 경로가 필요하다(작업 2).
5. **단계 ② 는 제로샷이었다.** PPO 는 단계 ①과 ②를 **섞어서** 학습한다(작업 3·6).
6. **단계 ① 은 시드를 바꿔도 같은 판이다**(액터 없음·항상 초록). 성적표는 서로 다른 판 수를 함께 적는다(작업 7, M3 성적표와 같은 방식).

## 어느 머신에서 하나 (M4b 의 두 머신 운용 중 이 부분만 앞당긴다)

| | 머신 | 왜 |
|---|---|---|
| 작업 1~6(코드·단위 테스트) | **lab-main** | 편집·테스트가 빠르고 무거운 계산이 없다 |
| 작업 3 걸음 속도 실측 | **두 머신 모두** | `--envs` 기본값을 정하는 근거 |
| 작업 7 본 학습 | **OMEN** | 32 코어(lab-main 16)라 병렬 환경이 2 배 |

**이 일은 GPU 가 아니라 CPU 가 병목이다.** 학생 정책 단일 환경 실측 1104 걸음/s 는 대부분 심판·세계·행 기록(파이썬)이고, 정책망은 256×256 이라 3090 Ti 가 3060 보다 크게 낫지 않다. OMEN 을 쓰는 이유는 **코어 수**다.

OMEN 준비 상태(2026-09-21 맞춰 둠): `~/vtd-rl-urban-driving` 에 공개 레포 클론, 서브모듈은 lab-main 에서 복사해 4a3f703 고정(OMEN 에는 비공개 레포 자격증명이 없다), M3 체크포인트·데이터도 **같은 경로**(`runs/lab-main/2026-09-17-dagger-fix2/`)로 복사됨, `.venv` 는 `uv` 로 만든 Python 3.10(시스템에 pip·venv·sudo 가 없다). 코드 갱신은 `git pull`, 결과 회수는 `rsync` 로 한다.

## 목표(성공 기준)

M3 학생의 실측치가 출발점이다 — 단계 ① 완주율 100%·점수 98.5, 단계 ② 완주율 100%·점수 94.2, **단계 ② 항목 ⑦(신호 위반) 중대 64건**, **단계 ① 항목 ②(보호구역 과속) 중대 15건**(구간 칸 수 기준, 평가 시드 3개 합).

| 항목 | 기준 |
|---|---|
| 완주율 | 단계 ①② 모두 **≥ 90%**(완주 못 한 판은 점수 0으로 친다) |
| 점수 | 단계 ①② 모두 선생님보다 **10점 이내**(완주 0점 처리 기준) |
| 신호 위반 | 단계 ② 항목 ⑦ 중대 **≤ 19건**(M3 대비 70% 이상 감소) |
| 보호구역 | 단계 ① 항목 ② 중대 **≤ 4건**(M3 대비 70% 이상 감소) |

네 줄을 모두 채우면 달성이다. 하나라도 못 채우면 **기준을 고치지 말고** 성적표에 숫자와 관찰을 적는다.

## 참고 자료(구현자 필독)

- `vtd_rl/env/drive_env.py` — `VtdDriveEnv(boards, config=None)`, Dict 관측/행동, `info["result"]`(`score`·`sheet`·`respawns`·`notes`), 종료 goal/collision/offroad, 중단 timeout/stalled. `EnvConfig(world, obs, action, reward, sections, use_map, log_dir)`.
- `vtd_rl/policy/net.py` — `DrivePolicy(PolicyConfig)`: `forward(vec, objs, mask) -> (mean[B,2], log_std[2], turn_logits[B,3])`, `act(obs, deterministic=True, generator=None)`, `save/load`, `.device`, `cfg.log_std_min`.
- `vtd_rl/policy/encode.py` — `VEC_DIM=73`, `OBJ_N=16`, `OBJ_DIM=12`, `flatten_obs`, `stack_obs`, `to_tensors`.
- `vtd_rl/policy/evaluate.py` — `EpisodeOutcome(board, seed, outcome, steps, reward, score, sheet)`, `run_policy_episode`, `evaluate_policy`, `evaluate_teacher`, `_summary`.
- `vtd_rl/policy/dataset.py` — `load_dir(path) -> DaggerDataset`(모방 손실용 M3 조각), `.batches(batch_size, generator, device)`.
- `vtd_rl/policy/train.py` — `policy_loss`(가우시안 NLL + 교차엔트로피) — 모방 손실은 이것을 재사용한다.
- M3 산출물: `runs/lab-main/2026-09-17-dagger-fix2/policy-r4.pt`(학생 체크포인트), `runs/lab-main/2026-09-17-dagger-fix2/data/*.npz`(라벨 20만 개).
- 실측: 환경 한 걸음 ≈ 2 ms(심판·행 기록 포함), 무작위 정책 1985 걸음/s, 판 하나 2500~5000 걸음.
- **gymnasium 1.3.0 의 벡터 환경은 `NEXT_STEP` 자동 리셋이다.** 판이 끝난 **다음 걸음**에서 행동이 무시되고 리셋이 일어나며, 보상 0·`terminated=False` 가 돌아온다. 그 한 걸음은 진짜 전이가 아니므로 **학습에서 빼야 한다**(작업 4의 `valid`, 작업 6의 `prev_done`). 빼지 않으면 "무엇을 해도 보상 0" 인 가짜 표본이 판마다 하나씩 섞인다.

## 파일 구조

| 파일 | 책임 |
|---|---|
| `vtd_rl/policy/net.py` (수정) | log_std 상·하한을 파라미터에 직접 적용(기울기 보존), 원표본+로그확률 API |
| `vtd_rl/policy/evaluate.py` (수정) | 완주 못 한 판은 점수 0 |
| `docs/superpowers/specs/…-design.md` (수정) | §6.4 진급 기준을 완주 조건부로 |
| `vtd_rl/rl/__init__.py` | 패키지 |
| `vtd_rl/rl/actor_critic.py` | `ActorCritic` — DrivePolicy + 가치 머리, M3 가중치 이식 |
| `vtd_rl/rl/vec_env.py` | 벡터 환경 만들기(판 섞기·시드) |
| `vtd_rl/rl/buffer.py` | 롤아웃 저장과 GAE |
| `vtd_rl/rl/ppo.py` | PPO 손실(정책·가치·엔트로피) + 모방 손실 감쇠 |
| `scripts/train_ppo.py` · `docs/reports/m4a-ppo.md` | 학습 실행과 완료 증거 |
| `tests/rl/…` | 단위·소형 학습 테스트 |

---

### Task 1: 정책망 손질과 점수 기준 수정

**Files:**
- Modify: `vtd_rl/policy/net.py`, `vtd_rl/policy/evaluate.py`, `docs/superpowers/specs/2026-09-15-vtd-rl-urban-driving-design.md`
- Create: `vtd_rl/rl/__init__.py`, `tests/rl/__init__.py`, `tests/rl/conftest.py`, `tests/rl/test_policy_fixes.py`
- Test: `tests/rl/test_policy_fixes.py`, 기존 `tests/policy/test_net.py`·`test_evaluate.py` 가 계속 통과해야 한다

**Interfaces:**
- Produces:
  - `PolicyConfig.log_std_max: float = 0.5` 추가. `forward` 는 **자르지 않은** `self.log_std` 를 그대로 돌려준다(기울기 보존)
  - `DrivePolicy.clamp_log_std()` — `torch.no_grad()` 로 파라미터를 `[log_std_min, log_std_max]` 안으로 in-place 조정한다. 최적화 한 걸음 뒤에 부른다
  - `DrivePolicy.sample(vec, objs, mask, generator=None) -> dict` — `{"raw": Tensor[B,2], "control": Tensor[B,2](자른 값), "turn": Tensor[B], "log_prob": Tensor[B], "entropy": Tensor[B], "value": None}`. `log_prob` 은 **자르기 전** 원표본의 가우시안 로그확률 합 + 지시등 범주형 로그확률
  - `DrivePolicy.evaluate_actions(vec, objs, mask, raw, turn) -> (log_prob[B], entropy[B])` — 저장해 둔 원표본에 대한 현재 정책의 로그확률(PPO 비율 계산용)
  - `evaluate._summary` 가 `mean_score` 를 **완주하지 못한 판은 0점**으로 집계한다. 원래 값은 `mean_score_raw` 로 함께 낸다
  - 스펙 §6.4 의 진급 기준 문장에 "완주하지 못한 판은 0점으로 집계한다"를 넣는다
- Consumes: 없음

- [ ] **Step 1: 실패하는 테스트 작성**

`tests/rl/__init__.py` — 빈 파일.

`tests/rl/conftest.py` — 여러 테스트 모듈이 함께 쓰는 재료. **`tests/__init__.py` 가 없어 `tests.rl.…` 로는 임포트가 안 되므로**, 헬퍼는 conftest 붙박이로 둔다.
```python
import pytest
import torch

from vtd_rl.env.observation import ObsConfig, observation_space
from vtd_rl.policy.encode import stack_obs, to_tensors
from vtd_rl.policy.net import PolicyConfig
from vtd_rl.rl.actor_critic import ActorCritic, ActorCriticConfig


@pytest.fixture
def obs_batch():
    def _make(n=4, seed=0):
        space = observation_space(ObsConfig())
        space.seed(seed)
        return to_tensors(*stack_obs([space.sample() for _ in range(n)]), torch.device("cpu"))
    return _make


@pytest.fixture
def small_ac():
    def _make():
        return ActorCritic(ActorCriticConfig(policy=PolicyConfig(trunk=(32, 32)),
                                             value_hidden=(32,)))
    return _make
```
작업 1 시점에는 `vtd_rl.rl.actor_critic` 이 아직 없다. **작업 1 에서는 conftest 에 `obs_batch` 만 쓰고**, `small_ac` 와 그 임포트는 작업 2 에서 더한다.

`tests/rl/test_policy_fixes.py`
```python
import torch

from vtd_rl.env.action import action_space
from vtd_rl.env.observation import ObsConfig, observation_space
from vtd_rl.policy.evaluate import EpisodeOutcome, _summary
from vtd_rl.policy.net import DrivePolicy, PolicyConfig


def test_log_std_는_기울기를_잃지_않는다(obs_batch):
    net = DrivePolicy(PolicyConfig(trunk=(32, 32)))
    vec, objs, mask = obs_batch()
    _mean, log_std, _logits = net(vec, objs, mask)
    log_std.sum().backward()
    assert net.log_std.grad is not None and torch.all(net.log_std.grad != 0.0)


def test_한_걸음_뒤에_잘라_범위를_지킨다(obs_batch):
    net = DrivePolicy(PolicyConfig(trunk=(32, 32), log_std_min=-2.0, log_std_max=0.5))
    with torch.no_grad():
        net.log_std.copy_(torch.tensor([-5.0, 3.0]))
    net.clamp_log_std()
    assert torch.allclose(net.log_std.detach(), torch.tensor([-2.0, 0.5]))
    # 바닥에 있어도 다음 기울기는 살아 있다
    _m, log_std, _l = net(*obs_batch())
    log_std.sum().backward()
    assert torch.all(net.log_std.grad != 0.0)


def test_표본은_자르기_전_로그확률을_준다(obs_batch):
    torch.manual_seed(0)
    net = DrivePolicy(PolicyConfig(trunk=(32, 32), log_std_max=2.0))
    with torch.no_grad():
        net.log_std.copy_(torch.tensor([1.5, 1.5]))     # σ≈4.5 — 표본이 확실히 잘린다
    vec, objs, mask = obs_batch(6)
    out = net.sample(vec, objs, mask, generator=torch.Generator().manual_seed(1))
    assert out["raw"].shape == (6, 2) and out["control"].shape == (6, 2)
    assert torch.all(out["control"] <= 1.0) and torch.all(out["control"] >= -1.0)
    assert out["turn"].shape == (6,) and out["log_prob"].shape == (6,)
    assert not torch.allclose(out["raw"], out["control"])      # 실제로 잘린 표본이 있다
    again, entropy = net.evaluate_actions(vec, objs, mask, out["raw"], out["turn"])
    assert torch.allclose(again, out["log_prob"], atol=1e-5)
    assert torch.all(entropy > 0.0)
    # 잘린 값이 아니라 원표본으로 계산한다
    clipped_lp, _ = net.evaluate_actions(vec, objs, mask, out["control"], out["turn"])
    assert not torch.allclose(clipped_lp, out["log_prob"], atol=1e-6)


def test_행동은_환경_공간_안이다():
    net = DrivePolicy(PolicyConfig(trunk=(32, 32)))
    space = observation_space(ObsConfig())
    space.seed(3)
    obs = space.sample()
    a = net.act(obs, deterministic=False, generator=torch.Generator().manual_seed(0))
    assert action_space().contains(a)


def test_완주하지_못한_판은_0점():
    eps = [EpisodeOutcome("A", 0, "goal", 100, 1.0, 98.0, []),
           EpisodeOutcome("B", 0, "stalled", 600, -5.0, 99.0, []),
           EpisodeOutcome("D", 0, "offroad", 90, -50.0, 100.0, [])]
    s = _summary(eps)
    assert s["goal_rate"] == 1 / 3
    assert s["mean_score"] == 98.0 / 3                       # 완주한 판만 점수를 남긴다
    assert s["mean_score_raw"] == (98.0 + 99.0 + 100.0) / 3
```

- [ ] **Step 2: 실패 확인**

Run: `env -u PYTHONPATH .venv/bin/pytest tests/rl/test_policy_fixes.py -q`
Expected: FAIL — `AttributeError: 'DrivePolicy' object has no attribute 'sample'` 등

- [ ] **Step 3: 구현**

`vtd_rl/policy/net.py`:
- `PolicyConfig` 에 `log_std_max: float = 0.5` 를 더한다(주석: 탐색 폭이 무한정 커지지 않게).
- `forward` 의 `log_std = self.log_std.clamp(min=self.cfg.log_std_min)` 을 `log_std = self.log_std` 로 바꾼다. 범위는 아래 `clamp_log_std()` 가 지킨다 — **clamp 를 forward 에 두면 바닥에서 기울기가 0이 되어 탐색 폭이 영원히 고정된다**(M3 성적표의 M4 인수인계 항목).
- 다음을 더한다:
```python
    @torch.no_grad()
    def clamp_log_std(self):
        """최적화 한 걸음 뒤에 부른다 — 범위는 지키되 기울기는 살려 둔다."""
        self.log_std.clamp_(self.cfg.log_std_min, self.cfg.log_std_max)

    def _dists(self, vec, objs, mask):
        mean, log_std, logits = self(vec, objs, mask)
        normal = torch.distributions.Normal(mean, log_std.exp())
        cat = torch.distributions.Categorical(logits=logits)
        return normal, cat

    def sample(self, vec, objs, mask, generator=None) -> dict:
        """PPO 용 표본 — **자르기 전** 원표본과 그 로그확률을 함께 준다.

        환경에는 자른 값을 넣지만, 로그확률·비율은 원표본으로 계산해야 분포가 일관된다.
        """
        normal, cat = self._dists(vec, objs, mask)
        noise = torch.randn(normal.mean.shape, generator=generator).to(normal.mean.device)
        raw = normal.mean + noise * normal.stddev
        turn = torch.multinomial(cat.probs.cpu(), 1, generator=generator).squeeze(-1).to(raw.device)
        log_prob = normal.log_prob(raw).sum(dim=-1) + cat.log_prob(turn)
        entropy = normal.entropy().sum(dim=-1) + cat.entropy()
        return {"raw": raw, "control": raw.clamp(-1.0, 1.0), "turn": turn,
                "log_prob": log_prob, "entropy": entropy}

    def evaluate_actions(self, vec, objs, mask, raw, turn):
        normal, cat = self._dists(vec, objs, mask)
        log_prob = normal.log_prob(raw).sum(dim=-1) + cat.log_prob(turn)
        entropy = normal.entropy().sum(dim=-1) + cat.entropy()
        return log_prob, entropy
```
- `act()` 는 그대로 두되 표본 경로는 `sample()` 을 쓰도록 정리한다(결정적 경로는 변화 없음).

`vtd_rl/policy/evaluate.py` 의 `_summary` 를 바꾼다:
```python
def _summary(episodes) -> dict:
    n = max(len(episodes), 1)
    # 완주하지 못한 판은 0점으로 친다 — 멈춰 선 차는 위반을 안 해 점수가 오히려 높다(M3 관찰).
    scored = [e.score if e.outcome == "goal" else 0.0 for e in episodes]
    return {"goal_rate": sum(1 for e in episodes if e.outcome == "goal") / n,
            "mean_score": sum(scored) / n,
            "mean_score_raw": sum(e.score for e in episodes) / n,
            "mean_reward": sum(e.reward for e in episodes) / n,
            "episodes": episodes}
```
기존 테스트는 이 변경으로 깨지지 않는다(확인함: `tests/policy/test_evaluate.py:37` 은 `mean_score <= 100.0`, `:43` 은 완주율 100% 인 경우라 값이 같다). 그래도 돌려서 확인한다. `scripts/run_dagger.py` 는 `mean_score` 를 M3 진급 기준에 쓰는데, 새 의미에서는 기준이 더 엄해진다 — 옳은 방향이므로 그대로 두고 **M3 성적표를 다시 만들지는 않는다**.

스펙 §6.4 의 진급 기준 문장에 한 줄 더한다:
```markdown
- 점수 집계에서 **완주하지 못한 판은 0점**으로 친다. 멈춰 선 차는 위반을 거의 안 해 점수가 높게 나오므로(M3 실측: 완주율 0%에 점수 97.6), 그대로 쓰면 점수 기준이 통과 못 할 수 없는 기준이 된다.
```

- [ ] **Step 4: 통과 확인**

Run: `env -u PYTHONPATH .venv/bin/pytest -q; echo rc=$?`
Expected: `rc=0`

- [ ] **Step 5: 커밋**

```bash
git add vtd_rl/policy/net.py vtd_rl/policy/evaluate.py docs/superpowers/specs vtd_rl/rl tests
git commit -m "M4a 준비 — log_std 는 한 걸음 뒤에 자르고(기울기 보존) 원표본 로그확률 API, 완주 못 한 판은 0점"
```

---

### Task 2: 행위자-비평가 망

**Files:**
- Create: `vtd_rl/rl/actor_critic.py`
- Modify: `tests/rl/conftest.py`(위 `small_ac` 붙박이를 여기서 더한다)
- Test: `tests/rl/test_actor_critic.py`

**Interfaces:**
- Consumes: `DrivePolicy`, `PolicyConfig`, `encode`
- Produces:
  - `ActorCriticConfig(policy=PolicyConfig(), value_hidden=(256, 256))`
  - `ActorCritic(cfg)` — `nn.Module`. 내부에 `self.policy = DrivePolicy(cfg.policy)` 와 **별도 가치 머리**(공유 몸통 위가 아니라 자체 MLP: 관측 벡터 + 물체 합치기 결과를 입력으로 받는 별도 경로 — 정책이 모방으로 이미 자리 잡았으므로 가치 학습이 그 몸통을 흔들지 않게 한다)
    - `.act(vec, objs, mask, generator=None) -> dict` — `policy.sample(...)` 결과에 `value: Tensor[B]` 를 더해 돌려준다
    - `.evaluate_actions(vec, objs, mask, raw, turn) -> (log_prob, entropy, value)`
    - `.value(vec, objs, mask) -> Tensor[B]`
    - `.clamp_log_std()` → 정책에 위임
    - `.save(path)` / `ActorCritic.load(path, device=None)`
  - `ActorCritic.from_policy(path, cfg=None, device=None) -> ActorCritic` — M3 체크포인트(정책만)를 읽어 정책 가중치를 이식하고 가치 머리는 새로 시작한다. 정책 설정은 체크포인트의 것을 쓴다

- [ ] **Step 1: 실패하는 테스트 작성**

`tests/rl/test_actor_critic.py`
```python
import os

import pytest
import torch

from vtd_rl.policy.net import DrivePolicy
from vtd_rl.rl.actor_critic import ActorCritic

M3_CKPT = "runs/lab-main/2026-09-17-dagger-fix2/policy-r4.pt"


def test_행동과_가치를_함께_낸다(small_ac, obs_batch):
    ac = small_ac()
    vec, objs, mask = obs_batch(5)
    out = ac.act(vec, objs, mask, generator=torch.Generator().manual_seed(0))
    assert out["value"].shape == (5,) and out["log_prob"].shape == (5,)
    lp, ent, val = ac.evaluate_actions(vec, objs, mask, out["raw"], out["turn"])
    assert torch.allclose(lp, out["log_prob"], atol=1e-5) and val.shape == (5,)
    assert torch.all(ent > 0.0)


def test_가치_학습이_정책_출력을_바꾸지_않는다(small_ac, obs_batch):
    ac = small_ac()
    vec, objs, mask = obs_batch(4)
    before = ac.policy(vec, objs, mask)[0].detach().clone()
    loss = ac.value(vec, objs, mask).pow(2).mean()
    loss.backward()
    torch.optim.SGD(ac.parameters(), lr=0.1).step()
    after = ac.policy(vec, objs, mask)[0].detach()
    assert torch.allclose(before, after, atol=1e-6)     # 가치 경로가 정책 몸통과 분리돼 있다


def test_저장하고_불러오면_같은_행동(small_ac, obs_batch, tmp_path):
    ac = small_ac()
    path = str(tmp_path / "ac.pt")
    ac.save(path)
    other = ActorCritic.load(path)
    vec, objs, mask = obs_batch(3)
    g1, g2 = torch.Generator().manual_seed(7), torch.Generator().manual_seed(7)
    a, b = ac.act(vec, objs, mask, generator=g1), other.act(vec, objs, mask, generator=g2)
    assert torch.allclose(a["raw"], b["raw"], atol=1e-6) and torch.equal(a["turn"], b["turn"])


@pytest.mark.skipif(not os.path.exists(M3_CKPT), reason="M3 체크포인트가 있어야 한다")
def test_M3_체크포인트를_이식한다(obs_batch):
    student = DrivePolicy.load(M3_CKPT)
    ac = ActorCritic.from_policy(M3_CKPT)
    vec, objs, mask = obs_batch(4)
    # 이식 뒤 log_std 를 범위 안으로 넣으므로(-2.0024 → -2.0) 평균만 같은지 본다
    assert torch.allclose(ac.policy(vec, objs, mask)[0], student(vec, objs, mask)[0], atol=1e-6)
    assert ac.policy.cfg.trunk == student.cfg.trunk
    assert ac.value(vec, objs, mask).shape == (4,)
```

- [ ] **Step 2: 실패 확인**

Run: `env -u PYTHONPATH .venv/bin/pytest tests/rl/test_actor_critic.py -q`
Expected: FAIL — `ModuleNotFoundError: No module named 'vtd_rl.rl.actor_critic'`

- [ ] **Step 3: 구현**

`vtd_rl/rl/actor_critic.py`
```python
"""행위자-비평가 — M3 정책망에 가치 머리를 붙인다.

가치 머리는 **정책 몸통을 공유하지 않는다**. 모방으로 이미 자리 잡은 정책을, 처음부터 배우는
가치 함수의 큰 기울기가 흔드는 것을 막기 위해서다(스펙 §6.3 "처음 몇 번은 가치 함수만 학습한다"와
같은 취지를 구조로도 지킨다).
"""
from dataclasses import asdict, dataclass, field

import torch
from torch import nn

from vtd_rl.policy.encode import OBJ_DIM, VEC_DIM
from vtd_rl.policy.net import NEG_BIG, DrivePolicy, PolicyConfig


@dataclass(frozen=True)
class ActorCriticConfig:
    policy: PolicyConfig = field(default_factory=PolicyConfig)
    value_hidden: tuple = (256, 256)
    obj_hidden: int = 64
    obj_out: int = 64


class ActorCritic(nn.Module):
    def __init__(self, cfg: ActorCriticConfig = ActorCriticConfig()):
        super().__init__()
        self.cfg = cfg
        self.policy = DrivePolicy(cfg.policy)
        self.v_obj = nn.Sequential(nn.Linear(OBJ_DIM, cfg.obj_hidden), nn.Tanh(),
                                   nn.Linear(cfg.obj_hidden, cfg.obj_out), nn.Tanh())
        layers, last = [], VEC_DIM + cfg.obj_out
        for width in cfg.value_hidden:
            layers += [nn.Linear(last, width), nn.Tanh()]
            last = width
        self.v_trunk = nn.Sequential(*layers)
        self.v_head = nn.Linear(last, 1)

    @property
    def device(self):
        return next(self.parameters()).device

    def value(self, vec, objs, mask):
        h = self.v_obj(objs).masked_fill(mask[..., None] <= 0.0, NEG_BIG)
        pooled = h.max(dim=1).values
        pooled = torch.where(mask.sum(dim=1, keepdim=True) > 0.0, pooled, torch.zeros_like(pooled))
        return self.v_head(self.v_trunk(torch.cat([vec, pooled], dim=-1))).squeeze(-1)

    def act(self, vec, objs, mask, generator=None) -> dict:
        out = self.policy.sample(vec, objs, mask, generator=generator)
        out["value"] = self.value(vec, objs, mask)
        return out

    def evaluate_actions(self, vec, objs, mask, raw, turn):
        log_prob, entropy = self.policy.evaluate_actions(vec, objs, mask, raw, turn)
        return log_prob, entropy, self.value(vec, objs, mask)

    def clamp_log_std(self):
        self.policy.clamp_log_std()

    def save(self, path: str):
        torch.save({"cfg": {"policy": asdict(self.cfg.policy),
                            "value_hidden": list(self.cfg.value_hidden),
                            "obj_hidden": self.cfg.obj_hidden, "obj_out": self.cfg.obj_out},
                    "state": self.state_dict()}, path)

    @classmethod
    def _cfg_from(cls, blob) -> ActorCriticConfig:
        c = blob["cfg"]
        pol = PolicyConfig(**{**c["policy"], "trunk": tuple(c["policy"]["trunk"])})
        return ActorCriticConfig(policy=pol, value_hidden=tuple(c["value_hidden"]),
                                 obj_hidden=c["obj_hidden"], obj_out=c["obj_out"])

    @classmethod
    def load(cls, path: str, device=None) -> "ActorCritic":
        blob = torch.load(path, map_location=device or "cpu", weights_only=False)
        net = cls(cls._cfg_from(blob))
        net.load_state_dict(blob["state"])
        if device is not None:
            net.to(device)
        return net

    @classmethod
    def from_policy(cls, path: str, cfg: ActorCriticConfig | None = None, device=None) -> "ActorCritic":
        """M3 체크포인트(정책만)를 이식한다 — 가치 머리는 새로 시작한다."""
        blob = torch.load(path, map_location=device or "cpu", weights_only=False)
        pol_cfg = PolicyConfig(**{**blob["cfg"], "trunk": tuple(blob["cfg"]["trunk"])})
        base = cfg or ActorCriticConfig()
        net = cls(ActorCriticConfig(policy=pol_cfg, value_hidden=base.value_hidden,
                                    obj_hidden=base.obj_hidden, obj_out=base.obj_out))
        missing, unexpected = net.load_state_dict(
            {f"policy.{k}": v for k, v in blob["state"].items()}, strict=False)
        assert not unexpected, unexpected
        assert all(k.startswith(("v_obj", "v_trunk", "v_head")) for k in missing), missing
        # M3 체크포인트의 log_std[0] 은 -2.0024 로 하한 밖이다(그때는 forward 에서 잘랐다).
        # 이제 forward 가 자르지 않으므로 여기서 한 번 범위 안으로 넣는다.
        net.clamp_log_std()
        if device is not None:
            net.to(device)
        return net
```

- [ ] **Step 4: 통과 확인**

Run: `env -u PYTHONPATH .venv/bin/pytest tests/rl -q; echo rc=$?`
Expected: `rc=0`

- [ ] **Step 5: 커밋**

```bash
git add vtd_rl/rl/actor_critic.py tests/rl/test_actor_critic.py
git commit -m "PPO — 행위자-비평가(가치 머리 분리, M3 정책 가중치 이식)"
```

---

### Task 3: 벡터 환경

**Files:**
- Create: `vtd_rl/rl/vec_env.py`
- Test: `tests/rl/test_vec_env.py`

**Interfaces:**
- Consumes: `VtdDriveEnv`, `EnvConfig`, `load_curriculum`, `gymnasium.vector`
- Produces:
  - `make_env_fn(curricula: tuple[str, ...], config: EnvConfig, seed: int, rank: int) -> Callable[[], VtdDriveEnv]` — 워커 안에서 커리큘럼을 읽어 판 목록을 만들고(단계 ①②를 **섞어서**) 환경 하나를 만든다. 판 선택은 환경 자신의 시드 난수가 한다
  - `make_vec_env(curricula, n_envs, config=None, seed=0, asynchronous=True)` — `gymnasium.vector.AsyncVectorEnv`(또는 `SyncVectorEnv`), `autoreset` 기본 동작을 쓰고, 반환 전에 `reset(seed=[seed+i ...])` 를 부른다
  - `vec_obs_to_arrays(obs_batch) -> (vec, objs, mask)` — 벡터 환경이 주는 Dict-of-arrays 를 `encode` 규약대로 `[N, …]` 배열로. `flatten_obs` 와 **같은 순서**여야 한다(테스트로 고정)
- 주의: 워커마다 지도(`rs.load_map()`)를 한 번 읽는다(0.34 초, 프로세스당 한 번). 워커 하나가 판 12개와 세계 캐시를 다 채워도 **139 MB** 뿐이라(2026-09-21 실측) 메모리는 제약이 아니다. 워커 수 기본값은 `max(1, cpu_count - 2)` 로 하고 `--envs` 로 덮어쓴다 — lab-main 14, OMEN 30(스펙 §6.5)

- [ ] **Step 1: 실패하는 테스트 작성**

`tests/rl/test_vec_env.py`
```python
import numpy as np
import pytest

from vtd_rl.policy.encode import OBJ_DIM, OBJ_N, VEC_DIM, flatten_obs
from vtd_rl.rl.vec_env import make_vec_env, vec_obs_to_arrays

STAGES = ("curricula/stage1.json", "curricula/stage2.json")


def test_동기_벡터_환경_한_걸음():
    venv = make_vec_env(STAGES, n_envs=2, seed=0, asynchronous=False)
    try:
        obs, info = venv.reset(seed=0)
        vec, objs, mask = vec_obs_to_arrays(obs)
        assert vec.shape == (2, VEC_DIM) and objs.shape == (2, OBJ_N, OBJ_DIM)
        assert mask.shape == (2, OBJ_N) and vec.dtype == np.float32
        action = venv.action_space.sample()
        obs, reward, term, trunc, info = venv.step(action)
        assert reward.shape == (2,) and term.shape == (2,)
    finally:
        venv.close()


def test_평탄화_순서가_단일_환경과_같다():
    venv = make_vec_env(STAGES, n_envs=1, seed=1, asynchronous=False)
    try:
        obs, _info = venv.reset(seed=1)
        vec, objs, mask = vec_obs_to_arrays(obs)
        single = {k: np.asarray(v[0]) for k, v in obs.items()}
        s_vec, s_objs, s_mask = flatten_obs(single)
        assert np.array_equal(vec[0], s_vec) and np.array_equal(objs[0], s_objs)
        assert np.array_equal(mask[0], s_mask)
    finally:
        venv.close()


def test_두_단계_판이_모두_등장한다():
    venv = make_vec_env(STAGES, n_envs=4, seed=3, asynchronous=False)
    try:
        signals = set()
        for _ in range(6):
            venv.reset()
            signals |= {e.unwrapped.board.signals for e in venv.envs}
        assert signals == {"always_green", "cycle"}
    finally:
        venv.close()


@pytest.mark.slow
def test_비동기_환경도_돈다():
    venv = make_vec_env(STAGES, n_envs=2, seed=0, asynchronous=True)
    try:
        venv.reset(seed=0)
        for _ in range(5):
            venv.step(venv.action_space.sample())
    finally:
        venv.close()
```

- [ ] **Step 2: 실패 확인**

Run: `env -u PYTHONPATH .venv/bin/pytest tests/rl/test_vec_env.py -q`
Expected: FAIL — `ModuleNotFoundError: No module named 'vtd_rl.rl.vec_env'`

- [ ] **Step 3: 구현**

`vtd_rl/rl/vec_env.py`
```python
"""벡터 환경 — 판을 여러 프로세스에서 동시에 굴린다.

판 목록은 단계 ①(항상 초록)과 ②(신호 주기)를 **섞는다**. M3 는 단계 ①만으로 배워 단계 ②가
제로샷이었고, 그래서 학생이 빨간불을 무시했다(M3 성적표). PPO 는 두 단계를 함께 본다.
"""
import os

import numpy as np
from gymnasium.vector import AsyncVectorEnv, SyncVectorEnv

from vtd_rl.env.drive_env import EnvConfig, VtdDriveEnv
from vtd_rl.policy.encode import VEC_KEYS
from vtd_rl.world.board import load_curriculum

REPO = os.path.join(os.path.dirname(os.path.abspath(__file__)), "..", "..")


def _boards(curricula):
    out = []
    for rel in curricula:
        path = rel if os.path.isabs(rel) else os.path.join(REPO, rel)
        _name, boards = load_curriculum(path)
        out += boards
    return out


def make_env_fn(curricula, config: EnvConfig | None, seed: int, rank: int):
    def _make():
        # 시드는 make_vec_env 의 reset(seed=[...]) 이 준다 — 여기서 리셋하면 세계를 한 번 더 짓는다.
        return VtdDriveEnv(_boards(curricula), config or EnvConfig())
    return _make


def make_vec_env(curricula, n_envs: int, config: EnvConfig | None = None, seed: int = 0,
                 asynchronous: bool = True):
    fns = [make_env_fn(tuple(curricula), config, seed, i) for i in range(n_envs)]
    venv = AsyncVectorEnv(fns) if asynchronous else SyncVectorEnv(fns)
    venv.reset(seed=[seed + i for i in range(n_envs)])
    return venv


def vec_obs_to_arrays(obs: dict):
    vec = np.concatenate([np.asarray(obs[k], dtype=np.float32).reshape(len(obs["ego"]), -1)
                          for k in VEC_KEYS], axis=1)
    return (vec, np.asarray(obs["objects"], dtype=np.float32),
            np.asarray(obs["object_mask"], dtype=np.float32))
```

- [ ] **Step 4: 통과 확인과 걸음 속도 실측**

Run: `env -u PYTHONPATH .venv/bin/pytest tests/rl/test_vec_env.py -q; echo rc=$?`
Expected: `rc=0`

실측(보고서에 적는다): 12개 비동기 환경에서 무작위 행동 2000 걸음을 돌려 초당 걸음 수를 잰다. 스크립트는 `/home/user/.claude/jobs/c5216a88/tmp` 에 둔다.

- [ ] **Step 5: 커밋**

```bash
git add vtd_rl/rl/vec_env.py tests/rl/test_vec_env.py
git commit -m "PPO — 벡터 환경(단계 ①② 섞기, 관측 평탄화 순서 고정)"
```

---

### Task 4: 롤아웃 버퍼와 GAE

**Files:**
- Create: `vtd_rl/rl/buffer.py`
- Test: `tests/rl/test_buffer.py`

**Interfaces:**
- Produces:
  - `RolloutBuffer(n_steps, n_envs, device)` — `.add(vec, objs, mask, raw, turn, log_prob, value, reward, done, valid)`, `.compute_gae(last_value, gamma=0.99, lam=0.95)`, `.batches(minibatch, generator)` → 평탄화된 미니배치 `(vec, objs, mask, raw, turn, old_log_prob, advantage, ret, old_value)`, `.reset()`
  - 이점은 배치 단위로 표준화한다(평균 0, 표준편차 1). `.advantages_raw` 는 표준화 **전** 값도 남긴다(로그용)
- 규칙: `done` 은 종료(terminated)만 부트스트랩을 끊는다. 중단(truncated)은 **가치로 부트스트랩**한다(시간 초과·정체는 판이 끝난 게 아니다)
- 규칙: `valid=0` 인 전이(자동 리셋 한 걸음)는 **미니배치에 넣지 않는다**. GAE 재귀는 바로 앞 걸음의 `done=1` 이 이미 끊어 주므로 그대로 두면 된다

- [ ] **Step 1: 실패하는 테스트 작성**

`tests/rl/test_buffer.py`
```python
import torch

from vtd_rl.rl.buffer import RolloutBuffer


def fill(buf, rewards, dones, values, n_envs=1, valids=None):
    valids = valids or [1.0] * len(rewards)
    for r, d, v, ok in zip(rewards, dones, values, valids):
        buf.add(vec=torch.zeros(n_envs, 73), objs=torch.zeros(n_envs, 16, 12),
                mask=torch.zeros(n_envs, 16), raw=torch.zeros(n_envs, 2),
                turn=torch.zeros(n_envs, dtype=torch.long), log_prob=torch.zeros(n_envs),
                value=torch.full((n_envs,), float(v)), reward=torch.full((n_envs,), float(r)),
                done=torch.full((n_envs,), float(d)), valid=torch.full((n_envs,), float(ok)))


def test_GAE_는_손으로_센_값과_같다():
    buf = RolloutBuffer(3, 1, torch.device("cpu"))
    fill(buf, rewards=[1.0, 1.0, 1.0], dones=[0.0, 0.0, 0.0], values=[0.0, 0.0, 0.0])
    buf.compute_gae(last_value=torch.zeros(1), gamma=1.0, lam=1.0)
    # 감쇠 없음·λ=1 이면 이점 = 남은 보상의 합
    assert torch.allclose(buf.advantages_raw.reshape(-1), torch.tensor([3.0, 2.0, 1.0]))
    assert torch.allclose(buf.returns.reshape(-1), torch.tensor([3.0, 2.0, 1.0]))


def test_종료는_부트스트랩을_끊는다():
    buf = RolloutBuffer(3, 1, torch.device("cpu"))
    fill(buf, rewards=[1.0, 1.0, 1.0], dones=[0.0, 1.0, 0.0], values=[0.0, 0.0, 0.0])
    buf.compute_gae(last_value=torch.zeros(1), gamma=1.0, lam=1.0)
    assert torch.allclose(buf.advantages_raw.reshape(-1), torch.tensor([2.0, 1.0, 1.0]))


def test_미니배치는_모든_표본을_한_번씩():
    buf = RolloutBuffer(4, 3, torch.device("cpu"))
    fill(buf, rewards=[0.0] * 4, dones=[0.0] * 4, values=[0.0] * 4, n_envs=3)
    buf.compute_gae(last_value=torch.zeros(3))
    seen = 0
    for batch in buf.batches(5, generator=torch.Generator().manual_seed(0)):
        seen += batch[0].shape[0]
        assert batch[0].shape[1] == 73 and batch[4].dtype == torch.long
    assert seen == 12


def test_자동_리셋_한_걸음은_배치에서_빠진다():
    buf = RolloutBuffer(3, 1, torch.device("cpu"))
    # t0 정상 → t1 종료 → t2 는 자동 리셋 더미
    fill(buf, rewards=[1.0, 1.0, 0.0], dones=[0.0, 1.0, 0.0], values=[0.0, 0.0, 0.0],
         valids=[1.0, 1.0, 0.0])
    buf.compute_gae(last_value=torch.zeros(1), gamma=1.0, lam=1.0)
    rows = sum(b[0].shape[0] for b in buf.batches(100, generator=torch.Generator().manual_seed(0)))
    assert rows == 2


def test_이점은_표준화된다():
    buf = RolloutBuffer(4, 2, torch.device("cpu"))
    fill(buf, rewards=[0.0, 1.0, 2.0, 3.0], dones=[0.0] * 4, values=[0.0] * 4, n_envs=2)
    buf.compute_gae(last_value=torch.zeros(2))
    adv = torch.cat([b[6] for b in buf.batches(100, generator=torch.Generator().manual_seed(0))])
    assert abs(float(adv.mean())) < 1e-5 and abs(float(adv.std()) - 1.0) < 1e-3
```

- [ ] **Step 2: 실패 확인**

Run: `env -u PYTHONPATH .venv/bin/pytest tests/rl/test_buffer.py -q`
Expected: FAIL — `ModuleNotFoundError: No module named 'vtd_rl.rl.buffer'`

- [ ] **Step 3: 구현**

`vtd_rl/rl/buffer.py`
```python
"""롤아웃 저장과 GAE.

시간 초과·정체(truncated)는 판이 끝난 게 아니므로 가치로 부트스트랩하고, 종료(terminated)만
끊는다 — 환경이 둘을 나눠 주므로(그 구분은 M2b 에서 배타적으로 만들어 두었다) 그대로 쓴다.
"""
import torch


class RolloutBuffer:
    def __init__(self, n_steps: int, n_envs: int, device):
        self.n_steps, self.n_envs, self.device = n_steps, n_envs, device
        self.reset()

    def reset(self):
        self._rows = []
        self.advantages_raw = None
        self.returns = None

    def add(self, **row):
        self._rows.append({k: v.detach().to(self.device) for k, v in row.items()})

    def _stack(self, key):
        return torch.stack([r[key] for r in self._rows])            # [T, N, ...]

    def compute_gae(self, last_value, gamma: float = 0.99, lam: float = 0.95):
        rewards, values, dones = self._stack("reward"), self._stack("value"), self._stack("done")
        adv = torch.zeros_like(rewards)
        running = torch.zeros(self.n_envs, device=self.device)
        next_value = last_value.to(self.device)
        for t in reversed(range(len(self._rows))):
            not_done = 1.0 - dones[t]
            delta = rewards[t] + gamma * next_value * not_done - values[t]
            running = delta + gamma * lam * not_done * running
            adv[t] = running
            next_value = values[t]
        self.advantages_raw = adv                     # [T, N]
        self.returns = adv + values                   # [T, N]
        return self.advantages_raw

    def batches(self, minibatch: int, generator=None):
        flat = {k: self._stack(k).flatten(0, 1) for k in self._rows[0]}
        keep = flat["valid"].nonzero(as_tuple=True)[0]        # 자동 리셋 더미를 뺀다
        flat = {k: v[keep] for k, v in flat.items()}
        adv = self.advantages_raw.flatten(0, 1)[keep]
        # 표본 표준편차(torch 기본)로 나눈다 — 결과의 표본 std 가 정확히 1 이 되어
        # test_이점은_표준화된다 의 단언과 맞고, CleanRL 등 PPO 구현의 관행이기도 하다.
        adv = (adv - adv.mean()) / (adv.std() + 1e-8)
        ret = self.returns.flatten(0, 1)[keep]
        order = torch.randperm(adv.shape[0], generator=generator)
        for start in range(0, adv.shape[0], minibatch):
            idx = order[start:start + minibatch].to(self.device)
            yield (flat["vec"][idx], flat["objs"][idx], flat["mask"][idx], flat["raw"][idx],
                   flat["turn"][idx], flat["log_prob"][idx], adv[idx], ret[idx],
                   flat["value"][idx])
```

- [ ] **Step 4: 통과 확인**

Run: `env -u PYTHONPATH .venv/bin/pytest tests/rl/test_buffer.py -q; echo rc=$?`
Expected: `rc=0`

- [ ] **Step 5: 커밋**

```bash
git add vtd_rl/rl/buffer.py tests/rl/test_buffer.py
git commit -m "PPO — 롤아웃 버퍼와 GAE(중단은 가치로 부트스트랩)"
```

---

### Task 5: PPO 손실과 모방 손실

**Files:**
- Create: `vtd_rl/rl/ppo.py`
- Test: `tests/rl/test_ppo.py`

**Interfaces:**
- Consumes: `ActorCritic`, `RolloutBuffer`, `vtd_rl.policy.train.policy_loss`(모방), `DaggerDataset`
- Produces:
  - `PPOConfig(lr=3e-4, clip=0.2, value_coef=0.5, entropy_coef=0.005, max_grad_norm=0.5, epochs=4, minibatch=1024, gamma=0.99, lam=0.95, imitation_coef0=1.0, imitation_half_life=2_000_000, target_kl=0.03)`
  - `ppo_losses(net, batch, cfg) -> (loss, parts)` — `parts` = `{policy, value, entropy, clip_frac, approx_kl}`. 정책 손실은 잘라낸 대리 목적, 가치 손실은 잘라낸 가치(옛 가치 기준 ±clip)
  - `imitation_coef(step, cfg) -> float` — `imitation_coef0 * 0.5 ** (step / imitation_half_life)`
  - `imitation_loss(net, dagger_batch, cfg) -> (loss, parts)` — M3 라벨에 대한 `policy_loss` 재사용(가우시안 NLL + 교차엔트로피)
  - `dagger_batches(dataset, batch_size, generator=None, device=None)` — **끝없이 도는** 배치 생성기. 롤아웃이 수백 번이라 한 바퀴로는 모자란다
  - `update(net, opt, buffer, dagger_iter, cfg, step, generator) -> dict` — 에폭 반복, 두 손실 합산, 기울기 자르기, `net.clamp_log_std()` 호출, `approx_kl > target_kl` 이면 그 에폭에서 멈춘다. 평균 지표 사전을 돌려준다

- [ ] **Step 1: 실패하는 테스트 작성**

`tests/rl/test_ppo.py`
```python
import torch

from vtd_rl.policy.dataset import DaggerDataset, Shard
from vtd_rl.policy.encode import OBJ_DIM, OBJ_N, VEC_DIM
from vtd_rl.rl.buffer import RolloutBuffer
from vtd_rl.rl.ppo import PPOConfig, dagger_batches, imitation_coef, ppo_losses, update
import numpy as np


def filled_buffer(net, n_steps=4, n_envs=2):
    buf = RolloutBuffer(n_steps, n_envs, torch.device("cpu"))
    g = torch.Generator().manual_seed(0)
    for t in range(n_steps):
        vec = torch.randn(n_envs, VEC_DIM)
        objs = torch.randn(n_envs, OBJ_N, OBJ_DIM)
        mask = torch.ones(n_envs, OBJ_N)
        out = net.act(vec, objs, mask, generator=g)
        buf.add(vec=vec, objs=objs, mask=mask, raw=out["raw"], turn=out["turn"],
                log_prob=out["log_prob"], value=out["value"],
                reward=torch.randn(n_envs), done=torch.zeros(n_envs),
                valid=torch.ones(n_envs))
    buf.compute_gae(last_value=torch.zeros(n_envs))
    return buf


def toy_dagger(n=64):
    rng = np.random.default_rng(0)
    ds = DaggerDataset()
    ds.add(Shard(rng.standard_normal((n, VEC_DIM), dtype=np.float32),
                 np.zeros((n, OBJ_N, OBJ_DIM), np.float32), np.zeros((n, OBJ_N), np.float32),
                 rng.uniform(-1, 1, (n, 2)).astype(np.float32),
                 rng.integers(0, 3, n).astype(np.int64), {"board": "toy"}))
    return ds


def test_처음_업데이트에서는_비율이_1이라_잘림이_없다(small_ac):
    net = small_ac()
    buf = filled_buffer(net)
    batch = next(iter(buf.batches(1000, generator=torch.Generator().manual_seed(0))))
    loss, parts = ppo_losses(net, batch, PPOConfig())
    assert abs(parts["approx_kl"]) < 1e-6 and parts["clip_frac"] == 0.0
    assert torch.isfinite(loss)


def test_모방_배치는_끝없이_돈다():
    it = dagger_batches(toy_dagger(8), 4, generator=torch.Generator().manual_seed(0))
    assert sum(1 for _ in zip(range(10), it)) == 10   # 두 배치짜리를 열 번 꺼내도 안 끊긴다


def test_모방_계수는_반감기로_준다():
    cfg = PPOConfig(imitation_coef0=1.0, imitation_half_life=100)
    assert imitation_coef(0, cfg) == 1.0
    assert abs(imitation_coef(100, cfg) - 0.5) < 1e-9
    assert abs(imitation_coef(200, cfg) - 0.25) < 1e-9


def test_업데이트가_돌고_범위를_지킨다(small_ac):
    net = small_ac()
    with torch.no_grad():
        net.policy.log_std.copy_(torch.tensor([-1.9, 0.45]))
    buf = filled_buffer(net, n_steps=8, n_envs=4)
    opt = torch.optim.Adam(net.parameters(), lr=1e-3)
    it = dagger_batches(toy_dagger(), 32, generator=torch.Generator().manual_seed(0))
    out = update(net, opt, buf, it, PPOConfig(epochs=2, minibatch=8), step=0,
                 generator=torch.Generator().manual_seed(0))
    assert set(out) >= {"policy", "value", "entropy", "approx_kl", "clip_frac", "imitation"}
    lo, hi = net.policy.cfg.log_std_min, net.policy.cfg.log_std_max
    assert torch.all(net.policy.log_std >= lo - 1e-6) and torch.all(net.policy.log_std <= hi + 1e-6)


def test_가치_손실이_가치를_목표로_당긴다(small_ac):
    net = small_ac()
    buf = filled_buffer(net, n_steps=8, n_envs=4)
    opt = torch.optim.Adam(net.parameters(), lr=3e-3)
    before = None
    for k in range(5):
        batch = next(iter(buf.batches(1000, generator=torch.Generator().manual_seed(k))))
        loss, parts = ppo_losses(net, batch, PPOConfig(entropy_coef=0.0))
        before = parts["value"] if before is None else before
        opt.zero_grad(set_to_none=True)
        loss.backward()
        opt.step()
    batch = next(iter(buf.batches(1000, generator=torch.Generator().manual_seed(0))))
    _loss, parts = ppo_losses(net, batch, PPOConfig(entropy_coef=0.0))
    assert parts["value"] < before
```

- [ ] **Step 2: 실패 확인**

Run: `env -u PYTHONPATH .venv/bin/pytest tests/rl/test_ppo.py -q`
Expected: FAIL — `ModuleNotFoundError: No module named 'vtd_rl.rl.ppo'`

- [ ] **Step 3: 구현**

`vtd_rl/rl/ppo.py`
```python
"""PPO 손실 + 모방 손실(감쇠) — 스펙 §6.3.

모방 손실은 M3 라벨(선생님)에 대한 로그가능도다. 처음에는 강하게 잡아 두고 반감기로 줄인다 —
PPO 단계에서는 선생님을 다시 돌리지 않고 모아 둔 데이터만 쓴다(스펙 §6.3).
"""
from dataclasses import dataclass

import torch
from torch import nn

from vtd_rl.policy.train import TrainConfig, policy_loss


@dataclass(frozen=True)
class PPOConfig:
    lr: float = 3e-4
    clip: float = 0.2
    value_coef: float = 0.5
    entropy_coef: float = 0.005
    max_grad_norm: float = 0.5
    epochs: int = 4
    minibatch: int = 1024
    gamma: float = 0.99
    lam: float = 0.95
    imitation_coef0: float = 1.0
    imitation_half_life: int = 2_000_000
    target_kl: float = 0.03


def imitation_coef(step: int, cfg: PPOConfig) -> float:
    return cfg.imitation_coef0 * 0.5 ** (step / max(cfg.imitation_half_life, 1))


def ppo_losses(net, batch, cfg: PPOConfig):
    vec, objs, mask, raw, turn, old_log_prob, adv, ret, old_value = batch
    log_prob, entropy, value = net.evaluate_actions(vec, objs, mask, raw, turn)
    ratio = (log_prob - old_log_prob).exp()
    unclipped = ratio * adv
    clipped = ratio.clamp(1.0 - cfg.clip, 1.0 + cfg.clip) * adv
    policy = -torch.min(unclipped, clipped).mean()
    v_clipped = old_value + (value - old_value).clamp(-cfg.clip, cfg.clip)
    value_loss = 0.5 * torch.max((value - ret) ** 2, (v_clipped - ret) ** 2).mean()
    ent = entropy.mean()
    loss = policy + cfg.value_coef * value_loss - cfg.entropy_coef * ent
    with torch.no_grad():
        approx_kl = float(((ratio - 1.0) - (log_prob - old_log_prob)).mean())
        clip_frac = float(((ratio - 1.0).abs() > cfg.clip).float().mean())
    return loss, {"policy": float(policy), "value": float(value_loss), "entropy": float(ent),
                  "approx_kl": approx_kl, "clip_frac": clip_frac}


def imitation_loss(net, dagger_batch, cfg: PPOConfig):
    return policy_loss(net.policy, dagger_batch, TrainConfig())


def dagger_batches(dataset, batch_size: int, generator=None, device=None):
    """M3 라벨을 끝없이 돌린다 — 롤아웃이 수백 번이라 한 바퀴로는 모자란다."""
    while True:
        for batch in dataset.batches(batch_size, generator=generator, device=device):
            yield batch


def update(net, opt, buffer, dagger_iter, cfg: PPOConfig, step: int, generator=None) -> dict:
    coef = imitation_coef(step, cfg)
    sums, count = {}, 0
    for _epoch in range(cfg.epochs):
        stop = False
        for batch in buffer.batches(cfg.minibatch, generator=generator):
            loss, parts = ppo_losses(net, batch, cfg)
            parts["imitation"] = 0.0
            if dagger_iter is not None and coef > 1e-4:
                try:
                    dbatch = next(dagger_iter)
                except StopIteration:
                    dbatch = None
                if dbatch is not None:
                    imi, iparts = imitation_loss(net, dbatch, cfg)
                    loss = loss + coef * imi
                    parts["imitation"] = iparts["total"]
            opt.zero_grad(set_to_none=True)
            loss.backward()
            nn.utils.clip_grad_norm_(net.parameters(), cfg.max_grad_norm)
            opt.step()
            net.clamp_log_std()
            for k, v in parts.items():
                sums[k] = sums.get(k, 0.0) + v
            count += 1
            if parts["approx_kl"] > cfg.target_kl:
                stop = True
                break
        if stop:
            break
    out = {k: v / max(count, 1) for k, v in sums.items()}
    out["imitation_coef"] = coef
    out["updates"] = count
    return out
```

- [ ] **Step 4: 통과 확인**

Run: `env -u PYTHONPATH .venv/bin/pytest tests/rl -q; echo rc=$?`
Expected: `rc=0`

- [ ] **Step 5: 커밋**

```bash
git add vtd_rl/rl/ppo.py tests/rl/test_ppo.py
git commit -m "PPO — 잘라낸 정책·가치 손실과 모방 손실 감쇠, KL 조기 종료"
```

---

### Task 6: 학습 스크립트

**Files:**
- Create: `scripts/train_ppo.py`
- Test: `tests/rl/test_train_ppo.py`

**Interfaces:**
- Consumes: `make_vec_env`, `vec_obs_to_arrays`, `ActorCritic`, `RolloutBuffer`, `PPOConfig`, `update`, `dagger_batches`, `load_dir`, `TrainConfig`(모방 배치 크기), `evaluate_policy`, `load_curriculum`
- Produces:
  - `scripts/train_ppo.py` — 인자 `--out --init runs/…/policy-r4.pt --curricula curricula/stage1.json curricula/stage2.json(기본값, 여러 개) --envs 12 --steps 3000000 --rollout 256 --warmup-updates 5 --eval-every 250000 --eval-seeds 3 --dagger-data runs/…/data --seed 0 --device auto --smoke`
    - 가치망 워밍업: 처음 `--warmup-updates` 번은 **정책 기울기를 끄고**(정책 파라미터 `requires_grad=False`) 가치·모방만 학습한다(스펙 §6.3)
    - 그 뒤 PPO. `--eval-every` 스텝마다 단계 ①② 평가 → `log.jsonl` 한 줄, 체크포인트 `ac-<스텝>.pt`, 최고 성적 `ac-best.pt`
    - **이름이 `ac-` 인 이유**: 저장되는 것은 `ActorCritic`(가치 머리 포함)이지 `DrivePolicy` 가 아니다. `policy-*.pt` 로 부르면 `DrivePolicy.load()` 로 읽힐 것처럼 보이는데 실제로는 못 읽는다 — `ActorCritic.save()` 가 쓰는 `cfg` 는 `{"policy": {...}, "value_hidden": [...], ...}` 라서 `PolicyConfig(**cfg)` 가 `TypeError` 를 낸다. 읽을 때는 `ActorCritic.load(path)`, 정책만 필요하면 `.policy` 를 꺼내 쓴다
    - `--smoke` 는 환경 2개·스텝 4000·롤아웃 64·동기 벡터 환경·평가는 **각 단계 코스 A 한 판씩 시드 1개**로 줄인 연습 모드다(몇 분 안에 끝나야 한다 — 전체 12판 평가는 판당 5~10초라 연습 모드에 넣지 않는다)
    - 출력 마지막 줄은 요약 JSON 하나. 같은 `--out` 에 두 번 쓰면 거부한다(M3 규칙)
- 주의: 벡터 환경은 `autoreset` 이라 판이 끝나면 다음 관측이 새 판의 것이다. `done` 은 `terminated` 만 쓰고 `truncated` 는 부트스트랩한다

- [ ] **Step 1: 실패하는 테스트 작성**

`tests/rl/test_train_ppo.py`
```python
import json
import os
import subprocess

import pytest

REPO = os.path.join(os.path.dirname(__file__), "..", "..")


@pytest.mark.slow
def test_연습_모드가_한_바퀴를_끝낸다(tmp_path):
    env = dict(os.environ)
    env.pop("PYTHONPATH", None)
    out = subprocess.run([os.path.join(REPO, ".venv", "bin", "python"),
                          os.path.join(REPO, "scripts", "train_ppo.py"),
                          "--smoke", "--out", str(tmp_path / "run"), "--seed", "0"],
                         capture_output=True, text=True, env=env, cwd=REPO, timeout=1800)
    assert out.returncode == 0, out.stderr[-3000:]
    summary = json.loads(out.stdout.strip().splitlines()[-1])
    assert summary["steps"] > 0 and "stage1" in summary
    assert os.path.exists(tmp_path / "run" / "log.jsonl")
    assert os.path.exists(tmp_path / "run" / "ac-best.pt")


@pytest.mark.slow
def test_같은_폴더에_두_번_쓰지_않는다(tmp_path):
    env = dict(os.environ)
    env.pop("PYTHONPATH", None)
    args = [os.path.join(REPO, ".venv", "bin", "python"),
            os.path.join(REPO, "scripts", "train_ppo.py"), "--smoke",
            "--out", str(tmp_path / "run")]
    first = subprocess.run(args, capture_output=True, text=True, env=env, cwd=REPO, timeout=1800)
    assert first.returncode == 0
    second = subprocess.run(args, capture_output=True, text=True, env=env, cwd=REPO, timeout=300)
    assert second.returncode != 0 and "이미" in (second.stderr + second.stdout)
```

- [ ] **Step 2: 실패 확인**

Run: `env -u PYTHONPATH .venv/bin/pytest tests/rl/test_train_ppo.py -q`
Expected: FAIL — 스크립트가 없어 종료 코드가 0 이 아니다

- [ ] **Step 3: 구현**

`scripts/train_ppo.py` 를 쓴다. 뼈대(핵심 흐름만; 인자 처리·로그·체크포인트는 M3 의 `scripts/run_dagger.py` 방식을 따른다):
```python
    cfg = PPOConfig()
    venv = make_vec_env(a.curricula, a.envs, EnvConfig(), seed=a.seed, asynchronous=not a.smoke)
    net = (ActorCritic.from_policy(a.init, device=dev) if a.init else ActorCritic()).to(dev)
    opt = torch.optim.Adam(net.parameters(), lr=cfg.lr)
    buf = RolloutBuffer(a.rollout, a.envs, dev)
    gen = torch.Generator().manual_seed(a.seed)
    dagger_iter = (dagger_batches(load_dir(a.dagger_data), TrainConfig().batch_size,
                                  generator=gen, device=dev) if a.dagger_data else None)
    obs, _info = venv.reset(seed=a.seed)
    updates = 0
    prev_done = np.zeros(a.envs, dtype=bool)     # 직전 걸음에 끝난 환경 = 이번 걸음은 자동 리셋 더미
    step = 0
    while step < a.steps:
        buf.reset()
        for _ in range(a.rollout):
            vec, objs, mask = (torch.as_tensor(x, device=dev) for x in vec_obs_to_arrays(obs))
            with torch.no_grad():
                out = net.act(vec, objs, mask, generator=gen)
            action = {"control": out["control"].cpu().numpy(), "turn": out["turn"].cpu().numpy()}
            obs, reward, term, trunc, info = venv.step(action)
            buf.add(vec=vec, objs=objs, mask=mask, raw=out["raw"], turn=out["turn"],
                    log_prob=out["log_prob"], value=out["value"],
                    reward=torch.as_tensor(reward, dtype=torch.float32),
                    done=torch.as_tensor(term, dtype=torch.float32),   # 중단은 부트스트랩
                    valid=torch.as_tensor(~prev_done, dtype=torch.float32))
            prev_done = np.asarray(term) | np.asarray(trunc)
            step += a.envs
        with torch.no_grad():
            vec, objs, mask = (torch.as_tensor(x, device=dev) for x in vec_obs_to_arrays(obs))
            last_value = net.value(vec, objs, mask)
        buf.compute_gae(last_value, gamma=cfg.gamma, lam=cfg.lam)
        warming = updates < a.warmup_updates          # 가치만 먼저(스펙 §6.3)
        for p in net.policy.parameters():
            p.requires_grad_(not warming)
        stats = update(net, opt, buf, dagger_iter if not warming else None, cfg, step,
                       generator=gen)
        for p in net.policy.parameters():
            p.requires_grad_(True)
        updates += 1
```
평가·기록·체크포인트는 `--eval-every` 스텝 경계마다. 평가는 `evaluate_policy(net.policy, boards, seeds)` 로 **결정적** 주행을 쓴다.

- [ ] **Step 4: 연습 모드 확인**

Run: `env -u PYTHONPATH .venv/bin/pytest tests/rl/test_train_ppo.py -q; echo rc=$?`
Expected: `rc=0`

- [ ] **Step 5: 커밋**

```bash
git add scripts/train_ppo.py tests/rl/test_train_ppo.py
git commit -m "PPO — 학습 스크립트(가치 워밍업 → PPO, 평가·체크포인트·로그)"
```

---

### Task 7: 본 학습과 성적표(M4a 완료 증거)

**Files:**
- Create: `scripts/report_m4a.py`(성적표 생성기), `docs/reports/m4a-ppo.md`(그 스크립트의 산출물)
- Modify: `README.md`
- Test: `tests/rl/test_report_m4a.py`

**Interfaces:**
- Consumes: `train_ppo.py` 가 남긴 `log.jsonl`·`ac-best.pt`, `evaluate_policy`, `evaluate_teacher`, `ActorCritic.load`, `DrivePolicy.load`(M3 체크포인트용)
- Produces:
  - `vtd_rl/policy/evaluate.py` 로 **옮겨 온** `violation_counts(ev) -> dict`(항목 → `{"minor": n, "major": n}`) — `scripts/run_dagger.py:89` 의 `_violation_counts` 를 그대로 옮기고, run_dagger 는 이제 이것을 임포트해 쓴다(복사본을 두지 않는다)
  - `scripts/report_m4a.py` — 인자 `--run runs/…/2026-09-17-ppo --m3 runs/lab-main/2026-09-17-dagger-fix2/policy-r4.pt --out docs/reports/m4a-ppo.md --eval-seeds 3 [--skip-eval]`. `--skip-eval` 은 평가를 건너뛰고 `log.jsonl` 만으로 뼈대를 만든다(테스트용)
- 성적표에 들어갈 것(전부 생성):
  - 머신·장치·시드·규칙 스택 커밋·총 스텝·걸린 시간
  - 스텝별 학습 곡선 표(평가 시점마다: 단계 ①② 완주율·점수(완주 0점 기준)·평균 보상·approx_kl·모방 계수)
  - **비교 표**: M3 학생 · M4a 학생 · 선생님 — 단계 ①② 완주율·점수, 그리고 **항목별 위반 표**(M3 성적표와 같은 형식)
  - 목표 네 줄(완주율·점수·항목 ⑦·항목 ②) 각각 달성/미달
  - 서로 다른 판 수(단계 ①은 시드 무관이라 6판), 학습에 쓴 커리큘럼
  - M4b 로 넘기는 것

- [ ] **Step 1: 실패하는 테스트 작성**

`tests/rl/test_report_m4a.py`
```python
import json
import os
import subprocess

import pytest

from vtd_rl.policy.evaluate import EpisodeOutcome, violation_counts

REPO = os.path.join(os.path.dirname(__file__), "..", "..")


def test_위반_세기는_구간_슬롯_단위():
    # 감점표의 항목 키는 **정수**다(score_fma.ITEMS 는 1~15 의 int).
    ev = {"episodes": [
        EpisodeOutcome("A", 0, "goal", 10, 0.0, 90.0,
                       [{7: "major"}, {7: "major", 2: "minor"}, {}]),
        EpisodeOutcome("B", 0, "goal", 10, 0.0, 95.0, [{2: "minor"}]),
    ]}
    counts = violation_counts(ev)
    assert counts[7] == {"minor": 0, "major": 2}
    assert counts[2] == {"minor": 2, "major": 0}


def test_M3_스크립트도_같은_함수를_쓴다():
    src = open(os.path.join(REPO, "scripts", "run_dagger.py"), encoding="utf-8").read()
    assert "from vtd_rl.policy.evaluate import" in src and "violation_counts" in src
    assert "def _violation_counts" not in src          # 복사본을 남기지 않았다


@pytest.mark.slow
def test_성적표가_필수_항목을_담는다(tmp_path):
    env = dict(os.environ)
    env.pop("PYTHONPATH", None)
    run_dir = str(tmp_path / "run")
    train = subprocess.run([os.path.join(REPO, ".venv", "bin", "python"),
                            os.path.join(REPO, "scripts", "train_ppo.py"),
                            "--smoke", "--out", run_dir, "--seed", "0"],
                           capture_output=True, text=True, env=env, cwd=REPO, timeout=1800)
    assert train.returncode == 0, train.stderr[-3000:]
    report = str(tmp_path / "m4a.md")
    made = subprocess.run([os.path.join(REPO, ".venv", "bin", "python"),
                           os.path.join(REPO, "scripts", "report_m4a.py"),
                           "--run", run_dir, "--out", report, "--skip-eval"],
                          capture_output=True, text=True, env=env, cwd=REPO, timeout=600)
    assert made.returncode == 0, made.stderr[-3000:]
    text = open(report, encoding="utf-8").read()
    for needed in ("PPO", "완주율", "항목별 위반", "목표", "시드", "커리큘럼"):
        assert needed in text, needed
    assert json.loads(open(os.path.join(run_dir, "log.jsonl"),
                           encoding="utf-8").readline())["step"] >= 0
```

- [ ] **Step 2: 실패 확인**

Run: `env -u PYTHONPATH .venv/bin/pytest tests/rl/test_report_m4a.py -q`
Expected: FAIL — `ImportError: cannot import name 'violation_counts'`

- [ ] **Step 3: 구현**

1. `scripts/run_dagger.py:89` 의 `_violation_counts` 를 `vtd_rl/policy/evaluate.py` 로 `violation_counts` 라는 이름으로 **옮긴다**(본문은 그대로). `run_dagger.py` 는 임포트해 쓰고, 그 자리의 정의는 지운다 — `_violation_lines` 는 run_dagger 에 남겨도 된다.
2. `scripts/report_m4a.py` 를 쓴다. 학습 곡선 표는 `log.jsonl` 에서, 비교 표와 항목별 위반 표는 세 가지 평가에서 만든다:
   - M4a 학생: `evaluate_policy(ActorCritic.load("<run>/ac-best.pt").policy, boards, seeds)` — **`DrivePolicy.load` 로는 못 읽는다**(위 작업 6 의 `ac-` 이름 설명 참고). `.policy` 를 꺼내면 `DrivePolicy` 라 `evaluate_policy` 가 그대로 받는다
   - M3 학생: `evaluate_policy(DrivePolicy.load(<m3 체크포인트>), boards, seeds)` — 이쪽은 순수 정책이라 `DrivePolicy.load` 가 맞다
   - 선생님: `evaluate_teacher(boards)` 목표 네 줄은 위 "목표(성공 기준)" 표의 숫자를 그대로 판정해 `달성`/`미달 (실측값)` 으로 적는다.

- [ ] **Step 4: 본 학습 실행 (OMEN)**

먼저 OMEN 에 코드를 올린다 — OMEN 은 이미 준비돼 있다(위 "어느 머신에서 하나" 참고).
```bash
git push origin <브랜치>
ssh 100.87.135.28 'cd ~/vtd-rl-urban-driving && git fetch -q origin && git checkout -q <브랜치> && git pull -q'
```
학습은 **앞단에서** 돌린다(뒤로 돌려놓고 보고를 잊는 일이 M3 때 여러 번 있었다). 오래 걸리니 `nohup` + 폴링이 아니라, 끝날 때까지 기다리는 한 번의 호출로 돌린다.
```bash
ssh 100.87.135.28 'cd ~/vtd-rl-urban-driving && env -u PYTHONPATH .venv/bin/python scripts/train_ppo.py \
  --out runs/omen/$(date +%F)-ppo --init runs/lab-main/2026-09-17-dagger-fix2/policy-r4.pt \
  --dagger-data runs/lab-main/2026-09-17-dagger-fix2/data \
  --envs 30 --steps 3000000 --seed 0' | tail -c 800
```
끝나면 결과를 lab-main 으로 회수한다(성적표는 lab-main 에서 만들어 커밋한다).
```bash
rsync -a 100.87.135.28:vtd-rl-urban-driving/runs/omen/ runs/omen/
```
그 다음 성적표를 만든다. 목표 네 줄 중 못 채운 것이 있으면 **기준을 고치지 말고** 숫자와 관찰(어느 코스에서 어떤 항목이 남는지, 학습 곡선이 어디서 꺾이는지)을 적는다. 한 번 더 돌릴 여지가 있으면(예: 스텝 2배, 엔트로피 계수 조정) 그 시도도 각각의 숫자와 함께 성적표에 남긴다.

- [ ] **Step 5: README 한 단락과 커밋**

```markdown
## 강화학습(PPO)
`vtd_rl/rl` 은 M3 모방학습 학생을 출발점으로 PPO 를 돌린다. 보상은 온라인 심판의 감점이고,
단계 ①(항상 초록)과 ②(신호 주기) 판을 섞어 학습한다. 결과: [docs/reports/m4a-ppo.md](docs/reports/m4a-ppo.md)

    env -u PYTHONPATH .venv/bin/python scripts/train_ppo.py --out runs/$(hostname)/$(date +%F)-ppo \
      --init <M3 체크포인트> --dagger-data <M3 데이터 폴더>
```

```bash
git add scripts/report_m4a.py docs/reports/m4a-ppo.md README.md tests/rl/test_report_m4a.py
git commit -m "M4a 증거 — PPO 학습 곡선·M3 대비 위반 감소·목표 판정"
```

---

## M4b 로 미루는 것(이 계획의 범위 밖)

- **커리큘럼 단계 ③④⑤**(사물·정지차 / 보행자·교통 / 연습코스 전체)와 자동 진급 — 액터가 있는 판을 만들어야 한다(M2a 의 시나리오 도구를 쓴다).
- **두 머신 운용의 나머지**(두 머신에서 동시에 다른 설정을 돌리고 결과를 모으기, `nice` 규칙) — 스펙 §6.5. M4a 는 "lab-main 개발 · OMEN 학습" 한 방향만 쓴다.
- 스텝 예산 계획과 하이퍼파라미터 탐색(엔트로피 계수·클립·롤아웃 길이).
- M3 성적표가 남긴 나머지 숙제: `act(deterministic=False)` 의 밀도 보정은 M4a 에서 고치지만, 학습 반복 재현성(같은 시드에서 같은 곡선)은 M4b 에서 확인한다.
